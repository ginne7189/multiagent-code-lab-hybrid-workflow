# 코드 실행 Skill

1. `make run`으로 정상·실패 결과를 먼저 확인합니다.
2. Trace에서 각 역할과 패턴의 실행 순서를 찾습니다.
3. `contracts/`의 필드와 실제 JSON 결과를 연결합니다.
4. `policies/`의 중단·사람 확인 조건과 실패 결과를 연결합니다.
5. `make check`로 정상·실패 계약이 유지되는지 확인합니다.

